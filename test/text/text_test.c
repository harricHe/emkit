#include "unity_fixture.h"
#include "text.h"

TEST_GROUP(text);

TEST_SETUP(text)
{
}

TEST_TEAR_DOWN(text)
{
}

TEST(text, separate)
{
}

TEST_GROUP_RUNNER(text)
{
	RUN_TEST_CASE(text, separate);
}

